/**
 * Takes an iterable of promises as an input, and returns a single Promise that resolves 
 * to an array of the results of the input promises.
 * 
 * @param {Array} values
 * @returns {Promise}
 */
const all = (values) => {}

module.exports = all